#version 450

layout(local_size_x = 1, local_size_y = 1, local_size_z = 1) in;

layout(r32ui, binding = 0) uniform uimage2D counterImg;
layout(std430, binding = 1) buffer pixCoord {
    vec4 pix[]; 
};


void main()
{
    pix[0].z = float(imageLoad(counterImg, ivec2(0)).x);
}
